// js_023_function.js

// 함수(function) : 특정 기능을하는 구무능ㄹ 독립된 부품으로 만들어 재사용하고자할 때 사용하는 구문이다.

/* 
함수 정의 
(1) 함수 선언문
function 함수명(매개변수){
    실행문;
    return값;
}

(2) g함수 표현식
let test = function(매개변수){
    실행문;
    return값;
}

(3) 즉시 실행(익명함수)
(function(파라미터)){
    console.log(`${파라미터}run~~`);

}
*/

let year = 2012;
// 함수 정의
function isLeapYear(year){
    let check;
    if()
}
